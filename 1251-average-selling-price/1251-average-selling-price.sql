# Write your MySQL query statement below
select a.product_id, round(sum((a.units * b.price)) / sum(units), 2) as average_price from UnitsSold a left join Prices b on a.product_id = b.product_id and a.purchase_date between b.start_date and b.end_date group by product_id